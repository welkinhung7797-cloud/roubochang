# CLAUDE.md — 肉搏場（Penguin Can Fight）

給 Claude Code 的專案指令。**開工前整份讀完**，這裡面每一條都是踩過坑換來的。

---

## 這是什麼

純前端 Canvas 的自動戰鬥 roguelite。企鵝格鬥家，20 波，撐過去就通關。
**零依賴**（沒有 npm、沒有 build），直接開 `index.html` 就能跑。

```
index.html      畫面骨架、CSS、面板
src/data.js     所有數值與定義（角色、武器、道具、敵人、連段盤、成就、同伴）
src/engine.js   模擬：戰鬥、招式、敵人 AI、波次、擊殺結算
src/render.js   繪製：角色幀、特效、粒子、UI 疊層
src/ui.js       面板、商店、結算、bot 測試入口（window.__test）
src/audio.js    音效與 BGM
tools/          量測與驗證腳本（Node / Python）
規格/           設計文件。★ 動手前先讀 爽度軸與50波成長設計.md 的 §10
assets/frames_<職業id>/   該職業的專屬動作幀（512×512 RGBA）
```

---

## ★★★ 第一鐵則：改完一定要跑機器閘門

```bash
node tools/驗證_同種子可重現.js
```

**這個必須是「同種子完全可重現」。不過就是壞的，不要交。**

為什麼這麼重要：整個專案的平衡判斷都建立在「同一顆種子跑出同一個結果」上。
一旦有跨局殘留狀態，所有量測數據同時失效，而且**不會報錯**——你只會看到數字莫名其妙地飄。

已經發生過一次：粒子特效用了戰鬥亂數，導致「有沒有噴粒子」會改變暴擊序列。
修之前，每一次調特效都在無聲重骰暴擊、掉寶、生怪。

其他閘門：

```bash
node tools/平衡_多種子通關率.js 3 10     # 危險3、每職業10種子
node tools/平衡_全職業快測.js            # 16 職業各跑一局
```

---

## ★★ 絕對不要做的事

1. **不要調平衡。** 總監 2026-08-05 明令：「我們的基準 先好玩比較重要 不要平衡」。
   數值差距（例如摔角手三格的 1.00×／1.50×／2.08×）是**已知且刻意保留**的，不是 bug。
   要動平衡必須先問。
2. **視覺相關的程式碼不准用 `rng()`。** 粒子／特效一律用 `prng()`（獨立亂數流）。
   混用會汙染模擬，見第一鐵則。
3. **不要把美術素材「烘死」。** 角色幀必須由 `tools/繪製企鵝.py` 這種可重跑的腳本產出，
   不要手工修圖存檔——之後改一個顏色就得整批重畫。
4. **不要在沒看畫面的情況下說視覺改動完成了。** 見下方「視覺 QA」。
5. **不要動 `assets/frames/`**（那是所有職業共用的基礎幀）。

---

## ★★ 這個專案最容易踩的五個坑（都發生過）

### 1. 判定與繪製是兩條獨立路徑
數值測試全過，畫面可以完全錯。實例：新招的八方向傷害全部正確、圈數正確、
確定性通過——然後開瀏覽器一看，旋翼是兩個藍紅色小方塊繞著角色轉
（裁圖裁到頭部面具）。**視覺項目沒有人用眼睛看過就不算通過。**

### 2. NaN 傷害會讓目標永遠殺不死，而且完全不報錯
`applyWeaponHit` 的暴擊分支吃 `w.critMult`。任何**臨時造出來的攻擊物件**
（特性、道具生成的 `G.strikes.push({ w: {...} })`）漏了欄位就會 `base *= undefined` → NaN，
而 NaN 的所有比較都是 false，那隻敵人從此不可能死。
→ 造攻擊物件時**每個數值欄位都要給齊**。已加最後閘門會攔截並寫進 `window.__test.errors`。

### 3. 同一個防護要放在唯一出口
`pushOutOfWalls` 有三個呼叫點。只在其中一個加條件，另外兩個會繞過去。
同樣的錯犯過兩次。**改行為時先 grep 全部呼叫點。**

### 4.「實測」兩個字只能寫在有數字有腳本的結論上
規格檔裡曾有一句「實測 0.40 秒窗口是硬牆、中段玩家一半接不到（§1-5）」——
查無此量測，§1-5 裡沒有任何相關數據。那句話被引用兩次，最後成為一個規則改動的依據。
**看到「實測」要追到數字本身，追不到就當它不存在。**

### 5. 拿工具去回答它答不了的問題
- bot 按 C 是為了衝刺不是為了接延伸技 → **bot 測不出「延伸技窗口該多寬」**
  （實測窗口 0.55／0.45／0.40 跑出逐位元相同的結果）
- bot 不主動搓連段 → **它的「幾秒發生一次」不能外推到真人**，要看轉換率
→ 下結論前先問：這個工具測得到我要問的東西嗎？

---

## 量測協定（報「單次輸出」一律附這五條）

單次施放的變異係數是 **49~78%**，所以：

1. **能用配對就用配對**——同一組場面跑不同設定，場面變異會被整條消掉，精度遠高於各量各的再相減
2. **非配對比較 n≥100**（n=36 時標準誤可以到 ±23）
3. 零 warm-up：`startRun` 之後不跑任何一幀
4. 假人擺位用**與遊戲分離的獨立亂數**，不要呼叫 `rng()`
5. 假人不死、`maxHp` 固定 150、觀測窗 ＝ 招式總長 + 2.5 秒

**通關率永遠拆三欄：通關／陣亡／逾時仍在 playing。**
只看一個百分比會漏掉「機制壞掉」——例如某職業通關率 70% 但**一次都沒死過**，
那 30% 全是卡關，是 bug 不是難度。

---

## 怎麼測（不用開瀏覽器）

`src/ui.js` 提供 `window.__test`。在 Node 用 `vm` 載入四個 src 檔就能跑無頭模擬：

```js
window.__test.simulate('wrestler', 3, 種子, 4200)   // 回傳 {result, wave, kills, hp, log}
```

`tools/` 底下每支腳本都是這個模式，複製一支改就好。

**bot 迴圈要照 `simulate` 的寫法**——非 playing 狀態要繼續呼叫 `botAutoUi()` 把商店關掉。
寫成 `while (G.mode === 'playing')` 會在第一次進商店就跳出，量出來的東西全是假的（踩過）。

---

## 視覺 QA（要看畫面的時候）

本機開 `python -m http.server 8746`，然後：

- **網址一定要帶 `?bf=1`**，否則走的是零件組合的另一條繪製路徑，改幀完全看不到效果
- 幀是非同步載入的，必要時直接呼叫 `loadBaseFrames()` / `loadCharFrames('wrestler')` 再等
- **不要從截圖反推畫布座標**（縮放與 DPR 會一直對不準）。用引擎自己的鏡頭：
  `畫布座標 = p.x - G.cam.x`
- `G.paused = true` 會讓鏡頭不再跟隨，畫面可能捲到空地。先放行讓鏡頭歸位再定格
- 要定格看某個招式：把它的 `dur` 設成很大、傷害設成 0，讓它一直畫著

---

## 美術規格

- 角色幀 **512×512 RGBA、透明背景**
- **水平置中 x ≈ 256、腳底 y ≈ 460**（錯了角色會浮空或陷進地板）
- 高度約 400~430 px
- 平塗、硬描邊，每個材質 3~4 階，不要漸層
- 定版設定稿：`assets/ref/企鵝設定稿_拳擊手.jpeg`（委託人朋友的小孩畫的，**設計不可改**）
- 企鵝本體與職業裝備要分層（本體 16 職業共用，換裝備就是換職業）

---

## 命名與語言

- **全部中文**：資料夾、文件、註解、招式名。程式碼變數用英文沒問題
- **禁簡體中文**
- 註解要寫**為什麼**，不是寫**做了什麼**。
  特別是「這個數字是量出來的、量的方法是什麼、推翻過什麼」——
  這個專案的註解密度偏高是刻意的，下一個人要靠它避免重踩

---

## 提交前檢查清單

- [ ] `node tools/驗證_同種子可重現.js` → 同種子完全可重現
- [ ] 有動到視覺 → **自己開瀏覽器看過**，附截圖
- [ ] 有動到數值 → 附配對量測數字，並註明 n
- [ ] 沒有引入新的 `rng()` 呼叫在繪製路徑上
- [ ] 新的攻擊物件欄位給齊（`dmg` `crit` `critMult` `lifesteal` `klass` `name`）
- [ ] 註解寫了「為什麼」跟「怎麼驗的」

---

## 找事做

`規格/爽度軸與50波成長設計.md` 的 §10.4「未決」列著待裁示項目。
**那些是總監的決定，不要自己動手**，可以先做分析與提案。

目前明確缺人的方向：
- 除了摔角手／劍豪／空手道之外，其他 13 個職業還沒做專屬連段盤與專屬幀
- 合氣道師範危險 3 只有 30% 通關，失敗全在前 4 波（已定位，是真的太弱，不是 bug）
- 同伴（影武者／弟子）目前不會重播摔投的完整演出，只重播衝擊圈
